# Copy to .env and fill in. Both values are the browser-safe publishable keys.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Shelby runs as Supabase Edge Functions (shelby, shelby-vision). Their
# ANTHROPIC_API_KEY is a Supabase function secret, never a client env var.
